spring:
    datasource:
        url: jdbc:mysql://localhost:3306/bank?useSSL=false&requireSSL=false&allowPublicKeyRetrieval=true&reWriteBatchedInserts=true
        username: root
        password: MysqlCool23-=
    jpa:
        properties:
            hibernate:
                dialect: org.hibernate.dialect.MySQLDialect
                jdbc:
                    batch_size: 1000
                order_inserts: true
                order_updates: true
        show-sql: true
        hibernate.ddl-auto: update
        defer-datasource-initialization: true
    liquibase:
        change-log: classpath:db/changelog/db.changelog-master.xml
        enabled: false
        drop-first: false
    sql:
        init:
            mode: always